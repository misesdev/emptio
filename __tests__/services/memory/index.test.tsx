import { getUser, insertUpdateUser } from '@src/services/memory/user'

describe("Memory data", () => {
    it("save user", () => {
        // insertUser({ userName: "mises" })

        // const { userName } = getUser()
        
        expect(true).toBe(true)
    })
})
